# 🚀 JARVIS v3.1 — Отчёт об обновлениях

**Дата:** 22 декабря 2025  
**Коммит:** `40b8433`  
**Репозиторий:** https://github.com/SashaZo1ton/bimar-presale-bot

---

## 🎯 Цель обновления

Исправить проблемы:
1. ❌ Генерировалось только 2 документа вместо выбранных 4
2. ❌ Документы в нередактируемых форматах (MD, DOCX вместо PDF)
3. ❌ Слабое обращение к базе знаний BIMAR

---

## ✅ Что сделано

### 1. **Изменены форматы документов**

| Документ | Было | Стало | Причина |
|----------|------|-------|---------|
| 01_Досье_на_клиента | `.docx` | `.pdf` | Удобство просмотра на мобильных |
| 04_Пилот_ТЗ | `.docx` | `.pdf` | Удобство просмотра на мобильных |
| 07_Верификация | `.docx` | `.pdf` | Удобство просмотра на мобильных |
| 02_Решения_BIMAR | `.xlsx` | `.xlsx` | Таблицы остаются редактируемыми |
| 03_Экономика_сделки | `.xlsx` | `.xlsx` | Таблицы остаются редактируемыми |
| 05_ЛПР_и_квалификация | `.xlsx` | `.xlsx` | Таблицы остаются редактируемыми |
| 06_Питч_для_клиента | `.pptx` | `.pptx` | Презентации остаются редактируемыми |

**Результат:** PDF для текстовых документов, редактируемые форматы для таблиц и презентаций.

---

### 2. **Создан файл `document_prompts.py`**

Отдельный промпт для каждого из 7 документов с:

#### 📚 **Усиленное обращение к базе знаний BIMAR**

Каждый промпт содержит секцию:

```
═══════════════════════════════════════════════════════════════
БАЗА ЗНАНИЙ BIMAR (ОБЯЗАТЕЛЬНО ИСПОЛЬЗОВАТЬ!)
═══════════════════════════════════════════════════════════════

📚 ОСНОВНЫЕ ФАЙЛЫ ПРОЕКТА BIMAR SYSTEM:

1. BIMAR_SYSTEM_Единая_база_знаний_2025-10-14.docx — ГЛАВНЫЙ ИСТОЧНИК
2. BIMAR_Модульная_структура_продаж.md — ЧТО ПРОДАЁМ
3. BIMAR-Логистика_Склад_Порт_СИЗ.pptx — КЕЙСЫ ЛОГИСТИКИ
4. BIMAR-РФ.pptx / BIMAR-РФ.pdf — ОБЩАЯ ПРЕЗЕНТАЦИЯ
5. Руководства пользователя (Android/Web)
6. BimAR × ОПТИКОМ — Пресейл-пакет.pdf

КРИТИЧЕСКИ ВАЖНО:
- ВСЕ данные о продуктах BimAR бери ТОЛЬКО из этих файлов
- НЕ придумывай функции или модули — используй реальные из базы знаний
- Цитируй конкретные разделы файлов при описании решений
```

#### 📋 **Структура промптов**

Каждый промпт включает:
- **Роль и цель** — что должен сделать Manus
- **Входные данные** — URL, цель встречи, ограничения
- **База знаний BIMAR** — какие файлы использовать
- **Структура документа** — детальное описание разделов
- **Требования** — формат, стиль, цитирование
- **Выходной файл** — точное название и формат

#### 📄 **Список промптов**

| ID | Документ | Промпт |
|----|----------|--------|
| `dossier` | 01_Досье_на_клиента.pdf | `PROMPT_DOSSIER` |
| `use_cases` | 02_Решения_BIMAR.xlsx | `PROMPT_USE_CASES` |
| `roi` | 03_Экономика_сделки.xlsx | `PROMPT_ROI` |
| `sow` | 04_Пилот_ТЗ.pdf | `PROMPT_SOW` |
| `stakeholders` | 05_ЛПР_и_квалификация.xlsx | `PROMPT_STAKEHOLDERS` |
| `presentation` | 06_Питч_для_клиента.pptx | `PROMPT_PRESENTATION` |
| `verification` | 07_Верификация.pdf | `PROMPT_VERIFICATION` |

---

### 3. **Изменена логика генерации документов**

#### ❌ **Было (v3.0):**

```
Этап 3: Один запрос в Manus API → все выбранные документы
Проблема: Manus не всегда генерировал все документы
```

#### ✅ **Стало (v3.1):**

```
Этап 3: Отдельный запрос для КАЖДОГО выбранного документа
Результат: Гарантированная генерация всех документов
```

#### 🔄 **Новая функция `process_selected_documents`:**

```python
async def process_selected_documents(message, state, user_id):
    selected_docs = data.get("selected_docs", [])
    all_artifacts = []
    
    # Генерируем каждый документ отдельно
    for idx, doc_id in enumerate(selected_docs, 1):
        # Получаем промпт для конкретного документа
        prompt = get_document_prompt(doc_id, url, goal, constraints)
        
        # Создаём задачу для этого документа
        task_id = await create_manus_task_single_doc(prompt)
        
        # Ожидаем завершения
        while True:
            task_status = await get_task_status(task_id)
            if status == "completed":
                # Извлекаем файлы
                all_artifacts.append(...)
                break
    
    # Отправляем все файлы пользователю
    for artifact in all_artifacts:
        await message.answer_document(...)
```

#### 🆕 **Новая функция `create_manus_task_single_doc`:**

```python
async def create_manus_task_single_doc(prompt: str) -> Optional[str]:
    """Создаёт задачу для генерации одного документа"""
    payload = {
        "prompt": prompt,
        "projectId": MANUS_PROJECT_ID
    }
    # Отправляет запрос в Manus API
```

---

### 4. **Обновлён `bot.py`**

#### Изменения:

1. ✅ Форматы документов в `DOCUMENT_TYPES`: `.docx` → `.pdf`
2. ✅ Импорт `document_prompts.py` в функциях генерации
3. ✅ Функция `create_manus_task_stage1` использует `get_document_prompt("dossier", ...)`
4. ✅ Функция `process_selected_documents` генерирует документы по одному
5. ✅ Функция `create_manus_task_single_doc` для отдельных запросов

---

## 📊 Результаты

### ✅ Решённые проблемы:

| Проблема | Решение | Статус |
|----------|---------|--------|
| Генерировалось только 2 документа | Отдельный запрос для каждого | ✅ Исправлено |
| Нередактируемые форматы | PDF для текста, XLSX/PPTX для таблиц | ✅ Исправлено |
| Слабое обращение к базе знаний | Явные инструкции в каждом промпте | ✅ Исправлено |

### 📈 Улучшения:

| Параметр | v3.0 | v3.1 |
|----------|------|------|
| Генерация всех документов | ❌ Не гарантирована | ✅ Гарантирована |
| Обращение к базе знаний | ⚠️ Общее | ✅ Детальное для каждого документа |
| Форматы файлов | ⚠️ Смешанные | ✅ Оптимизированные |
| Промпты | ⚠️ Один на все | ✅ Отдельный для каждого |

---

## 🚀 Деплой

**GitHub:** https://github.com/SashaZo1ton/bimar-presale-bot  
**Коммит:** `40b8433`  
**Ветка:** `master`

**Railway:** Автоматически перезапустит бота через 2-3 минуты после пуша.

---

## 📱 Как тестировать

1. Отправьте URL сайта компании
2. Выберите цель: "Вводная/квалификация"
3. Получите досье (5-10 мин) — **формат PDF**
4. Выберите нужные документы из меню (например, 4 документа)
5. Нажмите "Создать выбранные"
6. Получите **ВСЕ 4 выбранных файла** в правильных форматах

---

## 🔍 Что проверить

- [ ] Все выбранные документы генерируются
- [ ] Форматы файлов: PDF для текста, XLSX/PPTX для таблиц/презентаций
- [ ] В документах используется информация из базы знаний BIMAR
- [ ] Модули и функции BimAR — реальные, не выдуманные
- [ ] Есть цитирование источников

---

## 📝 Примечания

### Время генерации:

- **Этап 1 (досье):** 5-10 минут
- **Этап 3 (каждый документ):** 2-5 минут
- **Общее время для 4 документов:** 8-20 минут (последовательная генерация)

### Следующие улучшения (v3.2):

- [ ] Параллельная генерация документов (сократить время)
- [ ] Кэширование досье для повторных запросов
- [ ] Предпросмотр документов перед отправкой

---

**🤖 JARVIS v3.1 готов к тестированию!**
